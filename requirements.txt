# ── Core ML & NLP ─────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

# ── Classical NLP ─────────────────────────────────────────────────────────────
# TF-IDF + LinearSVC pipeline (models/tfidf_svc, models/tfidf_ensemble)
# → covered by scikit-learn above

# NB-SVM baseline (models/nbsvm)
# → covered by scikit-learn above

# ── fastText ──────────────────────────────────────────────────────────────────
# models/fasttext — requires NumPy < 2 because fastText's Python wrapper
# is not fully compatible with NumPy 2.x during predict()
fasttext-wheel>=0.9.2        # pip-installable fastText build

# ── Deep Learning / Transformers ──────────────────────────────────────────────
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0
tokenizers>=0.15.0

# ── SetFit ────────────────────────────────────────────────────────────────────
# models/setfit — to be completed
scikit-learn>=1.3.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
fasttext>=0.9.3
torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.28.0
tokenizers>=0.15.0
setfit>=1.0.0
evaluate>=0.4.0
scipy>=1.11.0
tqdm>=4.66.0

# ── BiLSTM + GloVe ────────────────────────────────────────────────────────────
# models/bilstm_glove — GloVe vectors loaded manually (not pip-installable)
# Download: https://nlp.stanford.edu/projects/glove/

# ── Evaluation & Utilities ────────────────────────────────────────────────────
evaluate>=0.4.0
scipy>=1.11.0
tqdm>=4.66.0

# ── Optional: experiment tracking ─────────────────────────────────────────────
# Uncomment if you use W&B or TensorBoard
# wandb>=0.16.0
# tensorboard>=2.15.0
